cities = []

# Ask the user to enter five city names
for i in range(5):
    city = input("Enter the name of a city: ")
    cities.append(city)

# Print the city names, one per line, in the same order
for city in cities:
    print(city)

